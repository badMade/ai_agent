{
  "name": "ai-agent-maintenance",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "healthcheck": "scripts/healthcheck.mjs",
    "self:heal": "scripts/self-heal.mjs",
    "lint": "echo \"No lint tasks are currently defined\"",
    "format": "echo \"No format tasks are currently defined\"",
    "typecheck": "echo \"No typecheck tasks are currently defined\"",
    "test": "echo \"No automated tests are currently defined\""
  }
}
